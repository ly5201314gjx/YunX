# 更新日志

## v1.0.2（2026-09-03）

### 修复：根治下载"前快后慢"
- 下载引擎强制 HTTP/1.1：此前同一域名的所有分片会多路复用进**同一条** TCP 连接，CDN 按连接累计字节限速时，全部分片一起降速——这就是"下到 60-70% 后越来越慢、直至下不动"的根因；
- 弹性区（后半段）每块请求强制轮换连接（`Connection: close`），规避 CDN 对"老连接"的隐性限速，后段速度曲线保持平坦；
- 修复上一版弹性区连接轮换参数从未真正启用的遗漏（编译与调用双双接上）；
- 末尾两级细块收尾（2MB / 512KB），最后一块即使只剩单连接也能快速收完，杜绝"末尾趴窝"；
- 断点续传防损坏：只保留连续字节前缀的分片，升级算法后自动清空旧分片网格重下。

## v1.0.1（2026-09-02）

### 优化
- 弹性区块大小统一与主池一致，前后段并发形态一致；
- 底部状态栏切换动画位移减半，切换更丝滑。

### 其它
- 首页品牌文案全面重写。

## v1.0.0（2026-09-01）

### 首个正式版
- 一键解析夸克 / UC / 迅雷 / 百度 / 123 / 139 分享链接，自动带出提取码；
- Range 分片多线程并发下载 + 断点续传；
- 下载管理：暂停 / 继续 / 删除 / 打开，进度与速度实时显示；
- 临时转存自动清理，不占用网盘空间；
- 剪贴板智能识别链接；液态玻璃 UI。